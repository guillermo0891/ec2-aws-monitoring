global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
    - job_name: 'node' 
      static_configs: {% for host in groups['targets'] | default([]) %}
        - targets: ['{{ hostvars[host].ansible_host }}:9100'] {% endfor %}